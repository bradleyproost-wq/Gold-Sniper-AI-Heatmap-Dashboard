# Gold Sniper AI Heatmap Dashboard - Render Fixed

## Render Settings

Build Command:
pip install -r requirements.txt

Start Command:
gunicorn app:app --bind 0.0.0.0:$PORT

## Test

/health
/api/state
/webhook
